{% extends "base.html.twig" %}

{% block title %}{{ titre }}{% endblock %}

{% block content %}
    <h1>{{ titre }}</h1>
    <div class="container">
        <form method="POST" action="/encode">
        <div class="d-flex justify-content-center">
            <div class="card border-warning mb-3" style="max-width: 30rem;">
                <div class="card-header">Encode</div>
                <div class="card-body">
                    <textarea class="form-control" name="encode" id="encode" rows="5" style="height: 100px;"></textarea>
                </div>
                <div class="text-center">
                    <button type="submit" class="btn btn-warning">Encode Message</button>
                    <div>
                        <h1>{{encode}}</h1>
                    </div>
                </div>
            </div>
        </div>
        </form>
    </div>
{% endblock %}